## tar + wasm + typescript example 

### dependencies

- rust (tested version 1.92.0)
- node (tested version 23.11.0)

### running

in project repo:

- `yarn install` or `npm install`
- `yarn serve` or `npm serve`

